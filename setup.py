#!/usr/bin/env python
# -*- coding: utf-8 -*-
from setuptools import setup, find_packages


setup(name='ucdcxr',
      version="1.0",
      description='UCD ChextX-ray14 project',
      packages=find_packages(exclude=[]),
      entry_points={
      },
      include_package_data=True,
      )
